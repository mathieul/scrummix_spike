// import {Socket} from "phoenix";
//
// let socket = new Socket("/ws");
// socket.connect();
// socket.join("topic:subtopic", {}).receive("ok", chan => console.log(chan));

let App = {};

export default App;
